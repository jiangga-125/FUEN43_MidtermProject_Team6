@model IEnumerable<ReportMail.Models.ReportExportLog>

@{
    ViewData["Title"] = "¶×¥X¬ö¿ý";
}

<h2 class="mb-3">¶×¥X¬ö¿ý</h2>

<p>
    <a asp-action="Create" class="btn btn-outline-secondary btn-sm">Create New</a>
</p>

<table class="table table-hover align-middle">
    <thead class="table-light">
        <tr>
            <th>@Html.DisplayNameFor(model => model.UserId)</th>
            <th>@Html.DisplayNameFor(model => model.ReportName)</th>
            <th>@Html.DisplayNameFor(model => model.ExportFormat)</th>
            <th>@Html.DisplayNameFor(model => model.ExportAt)</th>
            <th>@Html.DisplayNameFor(model => model.Filters)</th>
            <th>@Html.DisplayNameFor(model => model.FilePath)</th>
            <th style="width:180px;">¾Þ§@</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(_ => item.UserId)</td>
                <td>@Html.DisplayFor(_ => item.ReportName)</td>
                <td>@Html.DisplayFor(_ => item.ExportFormat)</td>
                <td>@(item.ExportAt?.ToString("yyyy/MM/dd HH:mm"))</td>
                <td><code class="small">@item.Filters</code></td>
                <td>@Html.DisplayFor(_ => item.FilePath)</td>
                <td class="d-flex gap-2">
                    <a class="btn btn-sm btn-primary"
                       asp-action="Download"
                       asp-route-id="@item.ExportId">¤U¸ü</a>

                    <a class="btn btn-sm btn-outline-secondary" asp-action="Details" asp-route-id="@item.ExportId">Details</a>
                    <a class="btn btn-sm btn-outline-primary" asp-action="Edit" asp-route-id="@item.ExportId">Edit</a>
                    <a class="btn btn-sm btn-outline-danger" asp-action="Delete" asp-route-id="@item.ExportId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
